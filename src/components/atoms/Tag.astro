---
interface Props {
  color?: string; // Optional hex or class override if needed, though we'll stick to a default for now.
}

// Current design uses a simple list with a dot. We can make this a pill tag if desired, 
// but based on "Habilidades Blandas" in AboutMe, it's a list item with a dot.
// Let's standardise it as a pill for better "atom" usage or keep the dot style but reusable.
// User requested "Atomic Design", usually Tags are pill shaped. Let's make it a nice pill tag.

const baseClasses = "flex items-center gap-2 px-3 py-1.5 rounded-md bg-white/40 dark:bg-white/5 border border-gray-200/50 dark:border-white/10 transition-colors hover:bg-white/60 dark:hover:bg-white/10";
---

<li class={baseClasses}>
  <div class="w-1.5 h-1.5 rounded-full bg-primary" aria-hidden="true" />
  <span class="text-sm text-gray-800 dark:text-txt-secondary">
    <slot />
  </span>
</li>
